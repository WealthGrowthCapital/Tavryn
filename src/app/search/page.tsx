export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q}=await searchParams;return <main className="container py-12"><h1 className="text-3xl font-semibold">Search</h1><form className="mt-6 flex max-w-2xl gap-2"><input name="q" defaultValue={q} placeholder="Search Tavryn" className="flex-1 rounded-xl border border-slate-300 px-4 py-3"/><button className="rounded-xl bg-slate-950 px-5 text-white">Search</button></form><div className="card mt-8 p-6"><p className="text-sm text-slate-600">{q?`Results for “${q}” will connect to Postgres search in the next production slice.`:'Enter a query to search answers, questions, and tools.'}</p></div></main>}
